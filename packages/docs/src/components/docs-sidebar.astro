---
import { getCollection } from 'astro:content'
import { docsPackageFromPathname, groupDocs, hrefFor, isCurrentPath, navLabel } from '@/lib/docs'
import { cn } from '@/lib/utils'

interface Props {
  class?: string
}

const pathname = Astro.url.pathname
const groups = groupDocs(await getCollection('docs'), docsPackageFromPathname(pathname))
---

<nav class={cn('min-w-0 text-sm', Astro.props.class)} aria-label="Documentation">
  {
    groups.map((group) => (
      <details open class="group/section mb-2 min-w-0">
        <summary class="text-muted-foreground hover:text-foreground flex cursor-pointer list-none items-center justify-between gap-2 rounded-md px-2 py-2 text-xs font-medium tracking-wide uppercase [&::-webkit-details-marker]:hidden">
          {group.label}
          <svg
            class="size-3.5 shrink-0 transition-transform group-open/section:rotate-180"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            aria-hidden="true"
          >
            <path d="m6 9 6 6 6-6"></path>
          </svg>
        </summary>
        <ul class="flex flex-col gap-0.5 pb-3">
          {group.entries.map((entry) => {
            const href = hrefFor(entry)
            const current = isCurrentPath(href, pathname)
            return (
              <li>
                <a
                  href={href}
                  aria-current={current ? 'page' : undefined}
                  class={cn(
                    'block min-w-0 rounded-md px-2 py-1.5 break-words transition-colors',
                    current
                      ? 'bg-sidebar-accent text-sidebar-accent-foreground font-medium'
                      : 'text-muted-foreground hover:bg-sidebar-accent hover:text-sidebar-accent-foreground',
                  )}
                >
                  {navLabel(entry)}
                </a>
              </li>
            )
          })}
        </ul>
      </details>
    ))
  }
</nav>
