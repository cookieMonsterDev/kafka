name: Setup Node.js and pnpm
description: Install Node 24, pnpm from packageManager, and workspace dependencies

runs:
  using: composite
  steps:
    - uses: pnpm/setup@v2
      with:
        runtime: node@24
        cache: true
        install: false
    - run: pnpm install --frozen-lockfile
      shell: bash
